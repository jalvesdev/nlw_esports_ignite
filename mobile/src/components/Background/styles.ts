import { StyleSheet } from 'react-native'
import { THEME } from '../../theme'

export const styles = StyleSheet.create({
  container: {
    flex: 1, // para que ocupe a tela toda
    backgroundColor: THEME.COLORS.BACKGROUND_800 // DEFINE A COR DE FUNDO
  }

})